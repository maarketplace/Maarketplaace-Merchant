export interface IResponseData {
    data: {
        data: {
            message: string;
            token: string
        }
        ststus: string;
        message: string;
        token: string
    }
}